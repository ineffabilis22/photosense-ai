# PhotoSense AI Design System

版本：Editorial Monochrome v2.1 — Newspaper Grid  
建立日期：2026-09-10  
最近迭代：2026-09-11  
适用范围：首页、开始点评、分析报告、历史记录、登录、注册，以及这些页面中的弹窗、表单、状态和导出前预览。

## 1. 参考依据与转译边界

本规范参考以下 UAL 当前页面与官方设计系统：

- [Architecture, spatial and interior design](https://www.arts.ac.uk/subjects/architecture-spatial-and-interior-design)：以大标题、图像、课程、设施、展示和故事构成连续的编辑页面。
- [UAL Space](https://www.arts.ac.uk/design-system/foundations/space)：将留白视为品牌与导航的一部分，使用 4–96 px 固定间距 Token，并强调通过空间关系完成分组和分隔。
- [UAL Layout](https://www.arts.ac.uk/design-system/foundations/layout)：使用有最大宽度的流动容器、12 栏内栅格和随宽度变化的栏间距。
- [UAL Typography](https://www.arts.ac.uk/design-system/foundations/typography)：字体应退居内容之后；字号与相邻文字间距随设备平滑缩放；标题行高约 110%，段落行高约 150%。
- [UAL Colour](https://www.arts.ac.uk/design-system/foundations/colour)：颜色按表面、内容、线条和交互角色使用，不让颜色脱离语义。

PhotoSense 只借鉴 UAL 的结构方法，不复制其品牌、字体或明亮配色。项目继续保持深色摄影编辑风格、橙红强调、真实用户照片和摄影学习语义。

本轮新增的报纸排版参考只用于提取结构规律：醒目的头条、导语、主图、两至四栏正文、统一栏距、少量功能性横线和图片注释。不得照搬参考图中的报头、英文文案、纸张颜色或装饰性复古元素。

## 2. 核心设计方向

PhotoSense AI 应像暗房工作台、摄影档案和报纸副刊的结合：直接、克制、有明确阅读顺序。层级主要由留白、字号、栏宽、对齐和内容顺序产生，边线只承担必要功能。

所有页面遵守以下原则：

1. 每个视口只保留一个第一焦点：照片、结论或当前主操作。
2. 用户照片保持原始色彩，不添加褪色、颗粒、网点、暗角或替代性滤镜。
3. 先用“距离差”表达关系：相关内容靠近，不相关内容拉开；不能默认给每个区块套框。
4. 同一层级只允许一种分隔方法。已经用留白或表面明度区分时，不再叠加边框和分割线。
5. 橙红只指向主操作、当前选择、键盘焦点、错误或最高优先级，不作普通装饰。
6. 长文本位于稳定、低纹理、高对比表面；纹理只存在于页面背景。

## 3. 设计元素与内容指向

| 设计元素 | 指向内容 | 禁止用法 |
| --- | --- | --- |
| 大尺度衬线标题 | 产品身份、页面名称、章节起点、照片名称 | 普通正文、按钮、筛选器、表单标签 |
| 无衬线正文 | 操作说明、摄影反馈、表单、导航、按钮、文件名和分类 | 为制造复古感而扩大中文字距 |
| 等宽数字 | 评分、步骤编号、日期、文件参数、档案序号 | “尚未选择文件”“数码摄影”等普通元数据、长标题和正文 |
| 橙红实底 | 当前页面唯一主操作 | 同屏多个高强度按钮、普通卡片背景 |
| 底部橙线 | 当前导航、选中选项、活动目录 | 选中按钮左侧竖线、无交互意义的装饰线 |
| 左侧橙线 | 错误、警告、来源说明或最高优先级提示 | 标签页、单选按钮、普通内容卡 |
| 深灰表面 | 需要整体阅读或操作的区域 | 卡片套卡片、每段正文单独成框 |
| 原始摄影图像 | 分析对象和视觉证据 | 改色滤镜、装饰图替代分析照片 |
| 次级灰文字 | 解释、标签、提示、辅助信息 | 关键结论、错误、主操作 |

## 4. 颜色系统

### 4.1 核心颜色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--em-canvas` | `#0b0b0b` | 页面背景、照片留黑 |
| `--em-surface` | `#151515` | 主要阅读面、报告面板、表单区 |
| `--em-surface-raised` | `#202020` | 输入框、次级控件、少量强调表面 |
| `--em-paper` | `#f5f5f1` | 主标题、关键结论、主要文字 |
| `--em-paper-soft` | `#e8e8e3` | 图片标题栏、按钮和强调文字 |
| `--em-dark-secondary` | `#a1a19a` | 正文说明、标签、元数据 |
| `--em-line-dark` | `#343434` | 控件边界、必要表格线、覆盖层边界 |
| `--em-accent-dark` | `#c86852` | 主按钮、选中态、重点建议、焦点 |
| `--em-accent-light` | `#a94430` | 深色背景上的小面积强调 |
| `--em-ink` | `#111111` | 橙色按钮文字、浅色状态文字 |

### 4.2 状态颜色

| 状态 | 颜色 | 使用规则 |
| --- | --- | --- |
| 成功 | `#6f9b7f` | 成功图标和短状态文字 |
| 警告 | `#c99a55` | 需要注意但不阻断的提示 |
| 错误 | `#c66a61` | 错误文字、图标和对应字段边界 |
| 禁用 | `#686863` | 不可操作控件，并配合文字与鼠标状态变化 |

不得只靠颜色传达状态；文本、图标、边线或 `aria` 状态必须至少补充一种。

## 5. 字体系统：全站最多三种字体角色

```css
--em-serif: 'Noto Serif SC', 'Source Han Serif SC', 'Songti SC', SimSun, Georgia, serif;
--em-sans: 'Noto Sans SC', 'Source Han Sans SC', 'Microsoft YaHei', 'Segoe UI', Arial, sans-serif;
--em-mono: 'IBM Plex Mono', 'Cascadia Mono', 'SFMono-Regular', Consolas, monospace;
```

| 角色 | 字体 | 字号 | 行高 | 字重 | 指向内容 |
| --- | --- | --- | --- | --- | --- |
| Display | 衬线 | `clamp(46px, 5vw, 72px)` | 0.95 | 500 | 首页产品名 |
| Page title | 衬线 | `clamp(32px, 4vw, 52px)` | 1.1 | 600 | 点评、报告、历史、登录和注册页标题 |
| Section | 衬线 | 20 px | 1.3 | 600–700 | 区块标题、照片标题、报告章节标题 |
| Body | 无衬线 | `clamp(16px, 0.25vw + 15px, 18px)` | 1.65 | 400–600 | 正文、报告结论、步骤说明 |
| UI / Metadata | 无衬线 | 14–16 px | 1.5 | 400–600 | 导航、按钮、标签、文件名、分类、表单辅助文字 |
| Numeric | 等宽 | 14 px 或 52 px | 1.2 | 400–600 | 步骤、日期、评分和技术参数 |

实施约束：

- “尚未选择文件”“数码摄影”“进阶水平”“街头摄影”等使用无衬线，不使用等宽字体。
- 等宽字体只能出现在数字或技术参数上；不得把包含中文的整行元数据统一设为等宽。
- 字重只使用 `400`、`600`、`700`；中文正文、标签和按钮的 `letter-spacing` 为 `0`。
- 标题和正文的间距由类型 Token 决定，不在单个组件里临时微调。

## 6. 间距系统：以留白代替套框

采用 UAL 的 4–96 px 固定间距范围，并映射到 PhotoSense Token：

```css
--em-space-1: 4px;
--em-space-2: 8px;
--em-space-3: 12px;
--em-space-4: 16px;
--em-space-6: 24px;
--em-space-8: 32px;
--em-space-10: 40px;
--em-space-12: 48px;
--em-space-16: 64px;
--em-space-20: 80px;
--em-space-24: 96px;
```

### 6.1 关系规则

必须满足：组件内距离 < 同组距离 < 跨组距离 < 大章节距离。

| 关系 | 间距 |
| --- | --- |
| 图标与文字、短标签之间 | 8 px |
| 标题与直接说明、标签与输入 | 12 px |
| 同一控件或卡片内部 | 16–24 px |
| 并列信息项、重复卡片之间 | 24–32 px |
| 相邻内容模块 | 40–48 px |
| 页面章节 | 64 px，移动端 40–48 px |
| 页面标题与主要内容、超大章节转换 | 80–96 px，移动端 48–64 px |

### 6.2 报纸式排版规则

- 先建立共同左边线和基线，再决定是否分栏。
- 大标题跨栏，正文限制行长；桌面中文正文建议 28–38 字，报告长文不超过约 65 个字符宽。
- 并列内容通过栏间距分开，不默认加竖线。
- 标题更靠近所属内容，而不是平均分布在上下空白中。
- 列数随可用宽度变化，但 DOM 阅读顺序不得改变。

## 7. 栅格与响应式

- 页面内容最大宽度：1320 px；使用 12 栏概念组织桌面内容。
- 页面左右安全区：`clamp(16px, 4vw, 48px)`。
- 桌面栏间距：`clamp(24px, 4vw, 48px)`；超宽屏不超过 64 px。
- 981 px 以上允许照片与控制面板并列。
- 761–980 px：复杂双栏改为单栏或主次上下排列，流程内容最多两列。
- 760 px 以下：正文不缩小换空间；模块间距收敛为 40–48 px。
- 420 px 以下：内容单列；触控目标至少 44 × 44 px；主操作紧随相关内容。
- 所有宽度禁止横向溢出、标题遮挡、固定元素覆盖操作。

## 8. 边线、表面和形状

### 8.1 边线保留清单

只允许以下边线：

1. 输入框、上传区、按钮等需要说明可操作范围的控件边界。
2. 表格、数据比较或图像裁切画布中具有实际对应关系的线。
3. 弹窗、覆盖层等需要从背景中脱离的外边界。
4. 活动导航或选项底部的 2–3 px 橙线。
5. 错误、警告、来源说明和最高优先级提示的单侧语义线。
6. 胶片穿孔、接触印相编号等明确指向摄影物件的线条。

### 8.2 必须删除的边线

- 普通内容卡的四边框。
- 卡片内部每段文字之间的分割线。
- 已经由背景明度或 32 px 以上留白分隔的重复线。
- 页面标题区、页头、页尾和章节之间的装饰性长线。
- 选中按钮左侧的橙线。
- 历史评分外的橙色方框。

普通容器圆角为 0；按钮、输入框和小控件圆角为 2 px。阴影只用于覆盖层，普通卡片不使用阴影。

## 9. 组件规则

### 9.1 页头与页面标题

- 所有页面使用同一黑色页头、衬线品牌、无衬线导航和底部活动橙线。
- 登录、注册和用户入口保持 44 px 最小触控尺寸。
- 页头、页面标题区和正文之间只用留白分层，不加装饰性分界线。
- 页面标题配一个不超过两行的说明，不放版本号或内部技术信息。

### 9.2 按钮与选择器

- 主按钮：橙色实底、深墨文字、2 px 圆角、最小高度 48 px。
- 主按钮悬停：黑色背景、橙色文字和橙色边界。
- 次按钮：透明背景、灰色边界；悬停使用白色或橙色边界。
- 选中选项：移除左侧竖线；使用 `inset 0 -3px 0 var(--em-accent-dark)` 或等价底部橙线。
- 未选中选项：可保留控制边界；若整个组已有明确背景，则取消每项外框并用间距分隔。
- 历史筛选中的未选标签使用 `--em-dark-secondary`，不得通过极低透明度隐藏；选中标签继续用暖白文字和底部橙线区分。
- 每页或每步骤最多一个高强度主按钮。

### 9.3 信息面板与报告

- 结论在前，证据在中，行动在后。
- 面板内部 24–32 px；相邻报告章节 64 px。
- 报告首页采用报纸头版式主次关系：左侧约 7 栏用于评审结论、导语、主要问题和本张先改，右侧约 5 栏用于综合评分、雷达图和五维分数；窄屏改为单栏。
- “主要问题 / 本张先改”并列占满结论栏宽，不能缩在左下角；两者只共享一条顶部功能线，不分别套框。
- “画面观察”的章节标题横跨内容区，下方用两栏组织四条观察，“裁剪参考”跨满整行；移动端按阅读顺序折叠为单栏。
- 报纸式栏网用于控制阅读顺序，不模拟纸张：正文保持左对齐、每栏约 20–34 个中文字符，栏距 32–64 px。
- 综合评分中的实心“优势项”标签使用 `--em-paper` 暖白文字；不得继承次级元数据灰色。
- “值得保留 / 最优先问题 / 画面区域 / 本张调整”使用无框两栏网格；“裁剪参考”可跨栏，但不再套大框。
- 五维诊断、后期建议和拍摄行动通过标题、编号、栏距和留白分组，不把每段做成边框卡。
- “本次评价基准”使用无框定义列表；四个维度通过列宽和 32–48 px 栏距区分。
- 示例、真实分析和 legacy 来源标签必须保留且清楚可见。

### 9.4 图片与档案

- 图片使用 `object-fit: contain` 或明确缩略图裁切，不改变原始色彩。
- 图片标题栏使用黑色半透明底；左侧衬线照片名，右侧无衬线属性。
- 历史卡使用照片、标题、日期和留白形成档案层级，不使用四边框卡片墙。
- 历史评分使用无框的大号等宽数字；橙色只强调数字或一条短底线。

## 10. 页面应用

### 首页

第一焦点是产品标题或当前照片。主介绍、照片与示例报告之间使用 48–64 px 节奏；流程步骤取消竖向分割线，以编号、对齐和栏间距组织。胶片结构线可保留，因为它具有摄影语义。

### 开始点评

第一焦点是待点评照片和上传状态。页面顺序固定为：作品信息 → 照片属性 → 上传确认 → 开始分析。选项的当前态统一使用底部橙线；文件状态和照片属性统一使用无衬线。工作台外框、步骤分割线和元数据顶线删除。

### 分析报告

第一焦点是综合结论和最优先行动。保留 01–05 目录和语义，但活动目录改为底部橙线。报告外框、章节底线、诊断卡四边框和评价基准套框删除；用 40–64 px 垂直节奏和 32–48 px 栏距重建层级。

分析报告的专用栏网、章节模板和响应式规则以 [`docs/REPORT_NEWSPAPER_LAYOUT_DESIGN.md`](docs/REPORT_NEWSPAPER_LAYOUT_DESIGN.md) 为准。该规范进一步定义横向目录索引、头版 8/4 图文结构、五维诊断 3+2 满栏结构和后期建议 7/5 图文结构；如与本段概述冲突，以专用规范为准。

### 历史记录

第一焦点是可复盘的历史作品。搜索、筛选、排序和管理保留必要控件边界；不提供记录对比模式。历史条目本身取消四边框，评分取消橙色方框。管理模式中的删除按钮使用与 `NEW` 一致的暗红实底、暖白文字和细亮边，确保不依赖悬停即可辨认；照片属性标签常态显示细边框。

### 登录与注册

第一焦点是单个表单。表单字段保留边界，承载表单的外层面板取消装饰性四边框。登录或注册提交按钮是唯一主按钮；演示功能说明必须保留。

## 11. 可访问性与验证

- 正文不低于 16 px；辅助文字不低于 14 px。
- 普通文字对比度至少 4.5:1；大文字至少 3:1。
- 键盘焦点使用 3 px 橙色轮廓和 3 px 外偏移。
- 选中状态除了底部橙线，还必须保留 `aria-current`、`aria-pressed`、单选状态或明确文本。
- 所有可操作元素使用语义元素；错误提示与对应字段建立关系。
- 支持 `prefers-reduced-motion`；减少动态时停用照片墙和胶片轨道动画。
- 每次视觉修改至少检查 1440、760、390 px，并运行 `npm.cmd run check`。
- 截图只能证明视觉布局；键盘、语义、状态和对比度仍需独立检查。

## 12. 禁止项

- 第四种字体或组件私有字体。
- 普通中文元数据使用等宽字体。
- 彩色渐变、黑金风格、玻璃拟态、大圆角卡片和普遍胶囊按钮。
- 卡片套卡片、每段一框、无意义横线或竖线。
- 选中状态使用左侧橙线。
- 改变用户照片色彩的滤镜。
- 用橙色填满多个同级区域。
- 低对比小字、长段居中文字、狭窄多栏正文。
- 删除或弱化真实分析、示例报告和旧评分来源标识。

## 13. 实现约定

- `design.md` 是视觉和内容语义的唯一指导文件。
- `src/theme-editorial-monochrome.css` 是当前代码 Token 与最终样式层。
- 新样式优先复用本文件 Token，不新增接近但不同的颜色、字号或间距。
- 页面结构和功能状态继续以 `src/App.tsx` 与自动化测试为准；视觉统一不得改变上传、分析、报告、历史和身份演示流程。
- 每次迭代先记录审查问题与对应选择器，再以最小覆盖层修改，禁止顺带重构页面结构。
