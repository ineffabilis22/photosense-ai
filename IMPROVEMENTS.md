# 本次迭代说明

## 核心体验 v3

- 支持点击或拖放上传 JPG、PNG、WebP，并增加空文件、格式和 15 MB 大小校验。
- 上传后显示文件大小，支持更换或移除照片；更换时保留点评参数和作品标题。
- 增加图片发送范围、浏览器历史和服务器保存策略说明。
- 分析过程拆分为准备图片、连接服务、分析画面、整理报告四个阶段，并显示等待时间和冷启动提示。
- 分析可取消；失败后可直接重试，取消不会生成报告或写入历史，重试不会重复保留失败的示例记录。
- 报告增加值得保留的优点、优先问题、画面区域、下一步动作、裁剪比例与方向，以及五项评分依据。
- 历史管理模式支持选择两份报告，比较总分、五项评分、主要问题、提升项和当前练习方向。
- 评价体系升级为“爱好者水平 / 进阶水平”两档；旧三档历史会按 schema 版本一次性清空，新两档历史继续使用安全兜底内容。

## 分析与报告

- 有效的模型观察、评分、结论和建议会被保留，只对缺失或无效字段使用兜底内容。
- 不再在前端对模型评分进行第二次机械校准，避免报告内容与评分互相矛盾。
- 分析结果明确标记为“实时 AI 分析”“示例报告”或“历史报告”。
- 网络失败、未配置 API、上游超时和无效响应会显示可理解的错误原因。
- 图片读取或压缩失败时不会生成或写入一条虚假的历史报告。

## 历史记录

- 缩略图使用压缩后的持久数据，不再保存刷新即失效的 `blob:` 地址。
- 媒介与题材筛选拆分，可组合使用；补齐日期、搜索和评分排序逻辑。
- “本月”统计按当前自然月计算。
- 历史记录最多保留 20 条，并标记示例报告或旧版未知来源。
- 统一历史卡片比例和最新标记颜色。

## 服务端与部署

- 增加 `/api/health` 健康检查。
- 超时、输出长度和温度等环境变量现在会真正生效，并具有安全边界。
- 公开生产环境默认关闭无账号保护的服务器端历史 JSON 写入。
- 可选历史快照限制为 20 份，避免无限增长。
- 启动脚本会自动读取项目根目录的 `.env`。
- `package-lock.json` 已与依赖版本同步，支持可复现的 `npm ci`。

## 质量保障

- 自动测试覆盖报告合并、上传校验、分析阶段、历史筛选/统计、报告对比、两档语言和服务端配置，共 42 项。
- 增加 DOM 交互验收，验证拖放上传、参数保留、取消不写历史、失败重试替换示例记录、旧三档历史清空、新两档语言和两份报告对比。
- 增加本地模拟 OpenAI-compatible 供应商的端到端测试，验证新提示词、图片输入、服务端归一化和照片针对性字段完整返回。
- `npm run check` 现在还会先对全部测试源码进行 TypeScript 类型检查。
- 新增 `npm run check`，一次完成测试、TypeScript 检查和生产构建。

登录、注册、分享及首页摄影素材未纳入本次修改范围。
