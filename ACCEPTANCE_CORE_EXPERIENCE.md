# 核心体验迭代验收标准

## 迭代 1：上传体验

- 支持点击和拖放上传 JPG、PNG、WebP。
- 拒绝不支持的文件类型、空文件和超过 15 MB 的文件，并显示明确原因。
- 上传后显示文件名、文件大小以及“更换照片”“移除照片”。
- 更换照片时保留媒介、题材、评价水平和作品标题；评价水平仅包含“爱好者水平”和“进阶水平”。
- 上传区域展示图片发送范围、浏览器历史与服务器保存策略说明。

## 迭代 2：分析过程

- 显示“准备图片、连接服务、分析画面、整理报告”四个阶段。
- 显示已经等待的秒数；等待较久时提示可能是部署冷启动。
- 分析过程中可以取消，取消后不生成示例报告、不写入历史。
- 失败后可以直接重试，并保留图片和全部点评参数。
- 成功、示例报告、错误、取消四种结果有明确且互不混淆的状态。

## 迭代 3：报告针对性

- 报告包含“值得保留的优点”“最优先问题”“画面区域”“下一步动作”。
- 五项评分均包含简短、用户可读的评分依据。
- 裁剪建议同时给出比例和方向；无法可靠判断时允许明确写“不建议裁剪”。
- 新字段缺失或无效时使用安全兜底，不覆盖模型已有的有效观察。
- 评价体系升级时一次性清空旧三档历史；升级后生成的两档历史记录仍能打开并正常显示。

## 迭代 4：历史报告对比

- 管理模式下可选择两份记录进行对比，不能选择超过两份。
- 对比页显示两张照片、总分差异、五项评分差异和主要问题。
- 自动指出变化最大的提升项与当前优先练习方向。
- v2 与 v3 记录允许并列查看，但必须提示评分标准不同，不计算总分或维度升降。
- 删除、筛选、查看单份报告等原有行为保持正常。

## 迭代 5：评分可信度

- 模型先按“作品级、强、成立、普通、偏弱、严重问题”选择五项等级，再由服务端映射为固定数值。
- “爱好者水平”和“进阶水平”使用同一基础视觉分；两档只改变解释语言的深浅。
- 五项均为“作品级”或“强”时，报告明确写“未发现影响画面成立的明显问题”，不为了提供建议而虚构缺陷。
- 最低等级为“成立”时，建议标记为可选优化，不把个人偏好描述成影响画面成立的问题。
- 历史记录保存评分版本；v2 记录继续可读并标记为旧评分标准。

## 自动验收

- `npm run test:typecheck` 通过，测试源码无 TypeScript 类型错误。
- DOM 交互测试覆盖拖放/更换/移除、取消、失败重试、旧记录和报告对比。
- 模拟 OpenAI-compatible 供应商的完整请求链路返回新增报告字段。
- `npm run test` 全部通过。
- `npm run build` 成功。
- 生产服务首页与 `/api/health` 返回成功。
- 空分析请求返回 400；生产环境历史写入保持 403。
