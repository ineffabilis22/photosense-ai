# 快速开始

```powershell
npm ci
Copy-Item .env.example .env
npm run server
```

另开一个终端：

```powershell
npm run dev
```

打开 `http://localhost:5173`。在运行分析前，请先按 [API_SETUP.md](./API_SETUP.md) 填写 `.env`；完整测试和部署说明见 [README.md](./README.md)。

